import 'package:flutter/material.dart';

class NotificationProfile extends StatelessWidget {
  const NotificationProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Expanded(child: Text('Notification'));
  }
}
