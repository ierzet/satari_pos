import 'package:flutter/material.dart';

class WindowTimeWidget extends StatelessWidget {
  const WindowTimeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Window Time');
  }
}
